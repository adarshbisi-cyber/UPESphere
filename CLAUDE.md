# CLAUDE.md

## Testing

- Run tests with `npm run test`.
- Component and unit tests live under `test/` and use `*.test.tsx` naming.
- See `TESTING.md` for conventions and examples.
- New functions, bug fixes, error handling, and conditional branches should include matching tests.
- Do not commit code that makes existing tests fail.
